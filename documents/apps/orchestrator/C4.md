# C4 Model — `apps/orchestrator`

## Level 1 — Context
- Orchestrator mengelola workflow agentic, koordinasi layanan, dan scheduling.

## Level 2 — Container
- Orchestrator ↔ API ↔ Workers ↔ Integrations ↔ Database ↔ Observability.

## Level 3 — Component
- Workflows (definitions, runner), Scheduler (cron/dynamic), Coordination (services/adapters), DAG lib, Allocator.

## Legend
- Persegi: modul; Silinder: database; Arah: aliran kontrol/data.

## Level 4 — Code Diagram
```
apps/orchestrator/
├─ workflows/{definitions,runner}
├─ scheduler/{cron,dynamic}
├─ coordination/{services,adapters}
├─ lib/{dag,allocator,retry}
└─ index.ts
```
