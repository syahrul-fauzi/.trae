# C4 Model — `apps/worker`

## Level 1 — Context
- Worker memproses job latar belakang dari queue dan berinteraksi dengan API serta integrasi.

## Level 2 — Container
- Worker runtime ↔ Queue ↔ API ↔ Integrations ↔ Observability.

## Level 3 — Component
- Jobs (etl, crmSync, notify), Retry lib, Queue adapter, Logger.

## Legend
- Persegi: modul; Awan: queue; Panah: alur data.

## Level 4 — Code Diagram
```
apps/worker/
├─ jobs/{etl,crmSync,notify}
├─ lib/{queue,logger,retry}
└─ config/{concurrency,policy}
```
