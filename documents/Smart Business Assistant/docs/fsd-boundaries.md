# FSD Boundaries — Frontend
- Struktur: `app → processes → widgets → features → entities → shared`
- Import rules: tidak ada impor naik; fitur hanya boleh impor entities/shared.
- Enforce via ESLint module boundaries.
